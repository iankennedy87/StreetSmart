source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '10.0'

target 'insightly' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for insightly
 
  pod 'GoogleMaps'
  pod 'GooglePlaces'
  pod 'Google/SignIn'

  target 'insightlyTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'insightlyUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
